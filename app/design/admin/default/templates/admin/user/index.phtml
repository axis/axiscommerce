<?php $this->headScript()->appendFile('/js/axis/admin/users.js'); ?>

<script type="text/javascript">
var roles = [
    <?php
    $roles = '';
    foreach ($this->roles as $id => $role) :
        $role = addslashes($role);
        $roles .= "[{$id}, '{$role}'], ";
    endforeach;
    echo substr($roles, 0, -2); ?>
];
</script>
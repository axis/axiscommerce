<?php 
echo $this->formSelect('currency-display', null, null, $this->currencyDisplays);
echo $this->formSelect('currency-position', null, null, $this->currencyPositions);
echo $this->formSelect('currency', null, null, $this->zendCurrencies);
echo $this->formSelect('locale', null, null, $this->zendLocales);
echo $this->formSelect('format', null, null, $this->zendLocales);
?>

<script type="text/javascript">
function renderDisplay(value){
    var values = Ext.decode('<?php echo Zend_Json::encode($this->currencyDisplays);?>');
    return values[value] ? values[value] : 'Default';  
};
    
function renderPosition(value){
    var values = Ext.decode('<?php echo Zend_Json::encode($this->currencyPositions);?>');
    return values[value] ? values[value] : 'Default';  
};

function renderLocale(value){
    var values = Ext.decode('<?php echo Zend_Json::encode($this->zendLocales);?>');
    return values[value] ? values[value] : value;  
};
</script>

<?php $this->headScript()->appendFile('/js/axis/admin/locale/currency.js'); ?>

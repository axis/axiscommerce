<?php
    $menu = $this->navigation()->menu($this->box->menu)
        ->disableWrapper(false)
        ->setOnlyActiveBranch(true)
        ->setRenderParents(false)
        ->render()
        ;
if (empty($menu)) {
    echo $this->translate('catalog')->__("No categories found");
} else {
    echo $menu;
} ?>